<Window x:Class="Report.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:local="clr-namespace:Report"
        mc:Ignorable="d"
        MouseMove="Window_MouseMove"
        Title="数据采集软件" FontSize="20" Height="600" Width="800" WindowState="Maximized">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Name="StartButton" Click="StartButton_Click" Grid.ColumnSpan="3">开始采集</Button>
        <Button Name="StopButton" Click="StopButton_Click" Grid.ColumnSpan="3" Visibility="Hidden">结束采集</Button>
        <CheckBox Name="ScaleCheckBox" IsChecked="True" Grid.Column="4" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="13">自动缩放图形</CheckBox>
        <ScottPlot:WpfPlot Name="WpfPlot1" Grid.Row="1" Grid.ColumnSpan="4" />
    </Grid>
</Window>
